export enum UserVerifyStatus {
  Unverified,
  Verified,
  Banned
}

export enum UserStatus {
  Official,
  Probationary,
  Collaborate,
  Resigned
}

export enum TokenType {
  AccessToken,
  RefreshToken,
  ForgotPasswordToken
}

export enum VerifyOP {
  Phone,
  Email
}
